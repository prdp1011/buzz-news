# ============ REQUIRED ============
# Database - Supabase pooler (Session + Transaction mode)
DATABASE_URL="postgresql://postgres.gymaifvqynzgdxvjfgdp:prdpposy-1011@aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.gymaifvqynzgdxvjfgdp:prdpposy-1011@aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres"

# Admin panel auth (required for admin app, min 32 chars)
JWT_SECRET="your-secure-random-secret-min-32-chars"

# ============ OPTIONAL ============
# Public site URL (for JSON-LD, sitemaps)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Worker

# Worker / Vercel Cron
CRON_INTERVAL_MS=3600000
CRON_SECRET=news-cron-setup-123
LOG_LEVEL=info

# AI (OpenAI) - omit for placeholder/fallback behavior
OPENAI_API_KEY=sk-proj-your-key-here
OPENAI_MODEL=gpt-4o-mini

# SSL (dev only - corporate proxies) - NEVER use in production
NODE_TLS_REJECT_UNAUTHORIZED=0
